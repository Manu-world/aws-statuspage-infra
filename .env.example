# Application
NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://statuspage:statuspage@db:5432/statuspage?schema=public
JWT_SECRET=change-me-to-a-long-random-string-in-production
COOKIE_SECURE=false

# Seed
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=ChangeMeStrongPassword1!
# true = wipe+insert demo services/incidents (local/CI). false = upsert admin only (prod boots).
SEED_DEMO_DATA=true
